{
  "scripts": {
    "generate": "npm run generate:txt && npm run generate:pdf",
    "generate:txt": "node generate-txt.js",
    "generate:pdf": "node generate-pdf.js",
    "format": "prettier --write \"**/*.{js,json,css,md,hbs}\""
  },
  "dependencies": {
    "jsonresume-theme-danivi-style": "file:danivi-style",
    "resumed": "^6.1.0",
    "puppeteer": "^24.36.1"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
